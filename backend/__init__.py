"""backend package initializer.

Creating this file makes the `backend` folder a Python package so imports
like `from backend.response import ...` work when running from other
working directories (for example, when Streamlit runs from `frontend`).
"""
