"""Frontend package for the Multi-Model RAG demo.

Run the UI with Streamlit:

PowerShell:
streamlit run frontend/ui.py

The UI uploads documents to `uploads/` and calls backend modules to
process and answer queries.
"""
